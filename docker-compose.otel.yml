version: '3.8'

services:
  # Jaeger All-in-One (OpenTelemetry 백엔드)
  # - UI: http://localhost:16686
  # - OTLP gRPC: localhost:4317
  # - OTLP HTTP: localhost:4318
  jaeger:
    image: jaegertracing/all-in-one:1.52
    container_name: semantica-jaeger
    ports:
      - "16686:16686"  # Jaeger UI
      - "4317:4317"    # OTLP gRPC (앱에서 사용)
      - "4318:4318"    # OTLP HTTP
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - SPAN_STORAGE_TYPE=memory  # 개발용 (프로덕션: badger, elasticsearch)
    networks:
      - otel-network
    restart: unless-stopped

  # Prometheus (선택사항 - 메트릭 수집)
  # - UI: http://localhost:9090
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: semantica-prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheus-data:/prometheus
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #   networks:
  #     - otel-network
  #   restart: unless-stopped

  # Grafana (선택사항 - 대시보드)
  # - UI: http://localhost:3000 (admin/admin)
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: semantica-grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   networks:
  #     - otel-network
  #   restart: unless-stopped

  # Langfuse Self-hosted (선택사항 - Cloud 사용 중)
  # Self-hosted 사용 시 아래 주석 해제
  # langfuse-server:
  #   image: langfuse/langfuse:latest
  #   container_name: semantica-langfuse
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=production
  #     - DATABASE_URL=postgresql://langfuse:langfuse@langfuse-db:5432/langfuse
  #     - NEXTAUTH_URL=http://localhost:3000
  #     - NEXTAUTH_SECRET=mysecret
  #     - SALT=mysalt
  #     - TELEMETRY_ENABLED=false
  #   depends_on:
  #     langfuse-db:
  #       condition: service_healthy
  #   networks:
  #     - otel-network
  #   restart: unless-stopped
  #
  # langfuse-db:
  #   image: postgres:15-alpine
  #   container_name: semantica-langfuse-db
  #   environment:
  #     - POSTGRES_USER=langfuse
  #     - POSTGRES_PASSWORD=langfuse
  #     - POSTGRES_DB=langfuse
  #   volumes:
  #     - langfuse-db-data:/var/lib/postgresql/data
  #   networks:
  #     - otel-network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U langfuse"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

networks:
  otel-network:
    driver: bridge

# volumes:
  # langfuse-db-data:  # Self-hosted 사용 시
  # prometheus-data:
  # grafana-data:
