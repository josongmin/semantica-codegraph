# 벤치마크 빠른 시작 가이드

## 가장 쉬운 방법

### 1줄 실행

```bash
# 방법 1: 셸 스크립트
./benchmark

# 방법 2: Python 직접
python benchmarks/run.py
```

대화형으로 진행되며, 질문에 답하기만 하면 됩니다.

## 명령어 모음

```bash
# 기본 (대화형)
./benchmark

# 빠른 테스트 (5개 쿼리)
./benchmark quick

# 정확도 평가
./benchmark eval

# Cody와 비교
./benchmark cody

# 전체 벤치마크
./benchmark full

# 도움말
./benchmark help
```

## 실행 흐름

### 단계 1: 실행
```bash
./benchmark
```

### 단계 2: 저장소 입력
```
📁 저장소 설정
  저장소 ID (기본: semantica-codegraph): [엔터]
```
그냥 엔터치면 기본값 사용

### 단계 3: 쿼리 선택
```
📝 테스트 쿼리 선택
  1. 기본 쿼리 5개 (빠름)
  2. 전체 쿼리 10개 (상세)
  3. 직접 입력
  선택 (1-3, 기본: 1): 1
```

### 단계 4: 결과 확인
```
[1/5] '설정 파일'
  ⏱️  145.3ms
    1. src/core/config.py
    2. src/core/bootstrap.py
    3. apps/api/main.py
```

### 단계 5: 통계 확인
```
📊 통계
총 쿼리:      5개
평균 응답:    145.3ms
가장 빠름:    98.2ms
가장 느림:    234.1ms
총 결과:      15개

✅ 응답 속도: 빠름 (200ms 미만)
```

끝!

## 예제 시나리오

### 시나리오 1: 첫 실행 (30초)
```bash
./benchmark

# 입력:
저장소 ID: [엔터]
선택: 1 [엔터]

# 결과 확인
# 5개 쿼리 자동 실행
```

### 시나리오 2: 정확도 체크 (1분)
```bash
./benchmark eval

# 자동으로:
# - 10개 쿼리 실행
# - 정답 데이터와 비교
# - Precision/Recall/MRR 계산
```

### 시나리오 3: 커스텀 쿼리 (2분)
```bash
./benchmark

# 입력:
저장소 ID: my-project
선택: 3

# 쿼리 입력:
> 사용자 인증
> 데이터베이스 연결
> API 엔드포인트
> [빈 줄]
```

## 문제 해결

### "초기화 실패"
```bash
# PostgreSQL/MeiliSearch 시작
docker-compose up -d

# 다시 실행
./benchmark
```

### "결과 없음"
```bash
# 저장소 인덱싱 확인
semantica list-repos

# 없으면 인덱싱
semantica index /path/to/repo
```

### "명령어를 찾을 수 없음"
```bash
# 권한 추가
chmod +x benchmark

# 또는 Python으로 직접
python benchmarks/run.py
```

## 출력 해석

### ✅ 좋음
- 응답 속도 < 200ms
- 모든 쿼리에 결과 있음
- Precision > 0.6

### ⚠️ 보통
- 응답 속도 200-500ms
- 일부 쿼리 결과 없음
- Precision 0.4-0.6

### ❌ 개선 필요
- 응답 속도 > 500ms
- 대부분 쿼리 결과 없음
- Precision < 0.4

## 다음 단계

1. **첫 실행**: `./benchmark` (30초)
2. **정확도 확인**: `./benchmark eval` (1분)
3. **Cody 비교**: `./benchmark cody` (5분)
4. **개선**: 가중치 조정 후 재실행

## 더 자세한 옵션

고급 사용자를 위한 옵션:

```bash
# 커스텀 쿼리 파일
python benchmarks/run.py --queries my_queries.txt

# 특정 저장소
python benchmarks/run.py --repo-id my-project

# 정확도 평가
python benchmarks/run.py --evaluate --repo-id my-project

# 완전한 제어
python -m benchmarks.compare --help
```

## 체크리스트

실행 전 확인:
- [ ] Docker 실행 중
- [ ] `docker-compose up -d` 완료
- [ ] 저장소 인덱싱 완료
- [ ] `.env` 파일 설정

실행 후 확인:
- [ ] 평균 응답 속도 확인
- [ ] 결과 품질 확인
- [ ] 개선점 파악

## 요약

**가장 쉬운 방법**:
```bash
./benchmark
```

입력만 하면 됩니다!
